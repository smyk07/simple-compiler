-include "io.scl"

int base
int exponent
int result = 1
int counter = 0

fasm "input %d", base
fasm "input %d", exponent

if exponent == 0 then goto :end

loop {
  result = result * base
  counter = counter + 1
  if counter < exponent then continue
  break
}

:end
fasm "output_int %d", result
